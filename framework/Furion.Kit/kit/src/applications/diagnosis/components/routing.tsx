import React from "react";
import { styled } from "styled-components";

const Container = styled.div``;

const Routing: React.FC = () => {
  return <Container>终点路由</Container>;
};

export default Routing;
