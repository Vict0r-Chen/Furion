import React from "react";
import styled from "styled-components";
import Page from "./page";

const Container = styled(Page)``;

const Logging: React.FC = () => {
  return <Container>运行日志</Container>;
};

export default Logging;
