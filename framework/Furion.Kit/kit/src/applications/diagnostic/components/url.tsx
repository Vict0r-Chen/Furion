import { styled } from "styled-components";
import TextBox from "../../../components/textbox";

const Url = styled(TextBox)`
  font-size: 14px;
  cursor: pointer;

  &:hover {
    color: #1677ff;
  }
`;

export default Url;
